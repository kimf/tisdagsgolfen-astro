---
import Button from '../ui/button.astro';
const userId = await Astro.session?.get('userId');
---

<Button variant="link">
  {!userId && <a href="/login">LOGGA IN</a>}
  {userId && <a href="/logout">LOGGA UT</a>}
</Button>
